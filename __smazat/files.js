






/*
*** práce se souborama
  // přečíst to nějak ze souboru
  //// todo - pokud soubor neexistuje, tak by ho to chtělo založit !
  loadJsonFile() {
    var pedo = fs.readFileSync(dbFile);
    var data = (pedo == "" ? {} : JSON.parse(pedo));

    return new stats.stats(data);
  }

  saveJsonFile() {
    fs.writeFile(dbFile, this.stats.getJson(), function (err) {
      if (err) {
        return console.log(err);
      }
    });
  }
  */


